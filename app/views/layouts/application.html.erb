<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= content_for(:title) || "WeaboTalk" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="WeaboTalk">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files (from builds and stylesheets) %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-slate-950">
    <!-- Navigation Bar -->
    <%= render "shared/navbar" %>

    <!-- Flash Notifications Container -->
    <div class="fixed top-20 left-0 right-0 flex flex-col items-center z-40 pointer-events-none">
      <% if notice.present? %>
        <div class="w-96 bg-emerald-900/80 border border-emerald-500/50 p-4 rounded-lg shadow-xl backdrop-blur-md animate-in fade-in slide-in-from-top duration-300 notification-toast pointer-events-auto" data-type="notice">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-emerald-400 mt-0.5 shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
            <div class="flex-1">
              <p class="text-emerald-100 font-semibold text-sm"><%= notice %></p>
            </div>
            <button class="close-notification ml-2 text-emerald-400 hover:text-emerald-300 shrink-0" onclick="this.closest('[data-type]').remove()">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
              </svg>
            </button>
          </div>
        </div>
        <script>
          setTimeout(() => {
            const notification = document.querySelector('[data-type="notice"]');
            if (notification) {
              notification.style.transition = 'opacity 300ms ease-in';
              notification.style.opacity = '0';
              setTimeout(() => notification.remove(), 300);
            }
          }, 4000);
        </script>
      <% end %>

      <% if alert.present? %>
        <div class="w-96 bg-rose-900/80 border border-rose-500/50 p-4 rounded-lg shadow-xl backdrop-blur-md animate-in fade-in slide-in-from-top duration-300 notification-toast pointer-events-auto mt-3" data-type="alert">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-rose-400 mt-0.5 shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
            </svg>
            <div class="flex-1">
              <p class="text-rose-100 font-semibold text-sm"><%= alert %></p>
            </div>
            <button class="close-notification ml-2 text-rose-400 hover:text-rose-300 shrink-0" onclick="this.closest('[data-type]').remove()">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
              </svg>
            </button>
          </div>
        </div>
        <script>
          setTimeout(() => {
            const notification = document.querySelector('[data-type="alert"]');
            if (notification) {
              notification.style.transition = 'opacity 300ms ease-in';
              notification.style.opacity = '0';
              setTimeout(() => notification.remove(), 300);
            }
          }, 5000);
        </script>
      <% end %>
    </div>

    <!-- Main Content (accounts for fixed navbar) -->
    <main>
      <%= yield %>
    </main>
  </body>
</html>
