<div class="min-h-screen bg-linear-to-br from-blue-50 to-indigo-100 flex items-center justify-center px-4 py-12">
  <div class="bg-white rounded-2xl shadow-2xl w-full max-w-md p-8">
    <!-- Header -->
    <div class="text-center mb-8">
      <div class="text-5xl mb-4">🔑</div>
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Forgot Your Password?</h1>
      <p class="text-gray-600">No problem! Enter your email and we'll send you instructions to reset it.</p>
    </div>

    <!-- Info Box -->
    <div class="bg-blue-50 border-l-4 border-blue-500 p-4 mb-6 rounded">
      <p class="text-blue-900 text-sm">
        We'll send a secure link to your email that expires in 6 hours. 
        Use it to create a new password.
      </p>
    </div>

    <!-- Form -->
    <%= form_with model: resource, as: resource_name, local: true, url: user_password_path, method: :post, html: { data: { controller: "form-validation", validation_url: validate_form_path, validation_type: "password_reset" } }, class: "space-y-4" do |form| %>
      <!-- Error Messages -->
      <% if resource.errors.any? %>
        <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-4 rounded">
          <h4 class="font-semibold text-red-800 mb-2">
            <%= pluralize(resource.errors.count, "error") %> found:
          </h4>
          <ul class="list-disc list-inside text-red-700 text-sm space-y-1">
            <% resource.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <!-- Email Field -->
      <div>
        <%= form.label :email, "Email Address", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.email_field :email,
            class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent",
            placeholder: "you@example.com",
            autofocus: true,
            autocomplete: "email",
            required: true %>
      </div>

      <!-- Submit Button -->
      <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 px-4 rounded-lg transition duration-200 mt-6">
        Send Reset Instructions
      </button>
    <% end %>

    <!-- Back to Sign In Link -->
    <div class="mt-6 text-center">
      <p class="text-gray-600 text-sm">
        Remember your password?
        <%= link_to "Sign in here", new_user_session_path, class: "text-indigo-600 hover:text-indigo-700 font-semibold" %>
      </p>
    </div>

    <!-- Additional Help -->
    <div class="mt-6 p-4 bg-gray-50 rounded-lg border border-gray-200">
      <p class="text-gray-700 text-xs font-semibold mb-2">💡 Tips:</p>
      <ul class="text-gray-600 text-xs space-y-1">
        <li>• Check your spam folder if you don't see the email</li>
        <li>• The reset link expires in 6 hours</li>
        <li>• You'll need to confirm your email before changing your password</li>
      </ul>
    </div>
  </div>
</div>
